//import VantusJS from VantusJS.js 

VantusJS.prototype.show = function(
    display = 'block', 
    func = 'linear', 
    speed = 0, 
    duration = 0)
{
    if(duration == 0){
        let h = this.css('height');
        console.log(h);
    }
}